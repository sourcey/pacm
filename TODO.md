1. Remote package cache (useable via command line app to avoid multiple queries).
2. Ensure packages don't overwrite existing installations.
3. Ensure no updates are active for current package before updaing.